-- =============================================================
--  캠핑 플랫폼 DB 마이그레이션 V26
--  변경     : camps.doNm 컬럼 제거 (3NF 위반: addr1 에 대한 이행적 종속)
--  사유     : 도(행정구역) 검색 필터용으로 V6 에서 추가했으나,
--            실제 검색 필터(CampRepository)는 addr1 LIKE 로 처리하고 있어
--            doNm 을 읽는 쿼리/서비스/프론트 코드가 전혀 없음 — 죽은 컬럼.
--            addr1 만 있으면 필요 시 파싱해서 쓸 수 있으므로 컬럼 자체를 제거한다.
-- =============================================================

ALTER TABLE camps
    DROP COLUMN doNm;
